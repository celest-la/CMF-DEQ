project: "ma_these_deq"
run_name: "baseline"

data:
  path: "data/h5/dataset_freq123.h5"

training:
  epochs: 50
  batch_size: 16
  learning_rate: 1e-3

model:
  frecon: 4.2e6
  dncnn_depth: 20
  params_algo:
    tau: 0.1
  max_iter: 20

wandb:
  enabled: true

save_path: "results"
